import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demo.childrenbookreader',
  appName: 'Children Book Reader',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
